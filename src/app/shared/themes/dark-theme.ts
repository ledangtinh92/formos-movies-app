import { Theme } from './symbols';

export const darkTheme: Theme = {
  name: 'dark',
  properties: {
    '--background': '#303030',
    '--on-background': '#fff',
    '--primary': '#fff',
    '--on-primary': '#fff',
  }
}
